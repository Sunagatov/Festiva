# =============================================================================
# Festiva — Local Development Environment
# =============================================================================
# All values here work out of the box for local development.
# Start the infrastructure first:
#   docker compose up -d mongo
# Then run the app from IntelliJ or:
#   export $(cat .env | xargs) && mvn spring-boot:run
# =============================================================================


# -----------------------------------------------------------------------------
# MongoDB  (matches the 'mongo' Docker container)
# -----------------------------------------------------------------------------
MONGO_URI=mongodb://localhost:27017
MONGO_DATABASE_NAME=FestivaDatabase


# -----------------------------------------------------------------------------
# Telegram Bot
# -----------------------------------------------------------------------------
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=


# -----------------------------------------------------------------------------
# Kafka metrics  (disabled by default — set APP_KAFKA_ENABLED=true when needed)
# -----------------------------------------------------------------------------
APP_KAFKA_ENABLED=false
KAFKA_BOOTSTRAP_SERVERS=
KAFKA_API_KEY=
KAFKA_API_SECRET=


# -----------------------------------------------------------------------------
# AI (optional — set AI_ENABLED=true and provide OPENAI_API_KEY to enable)
# -----------------------------------------------------------------------------
AI_ENABLED=false
OPENAI_API_KEY=
