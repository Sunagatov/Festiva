# Festiva Copilot Instructions

Use `AGENTS.md` as the source of truth for this repository.

Do not duplicate agent rules here. Keep shared AI-agent workflow in `AGENTS.md`
and task-specific source facts in `docs/ai/`.
